# SDD planning artifacts (local only, not for the client repo)
openspec/

# Local tooling state (machine-specific, not for the shared repo)
.atl/
.claude/settings.local.json

# OS / editor
.DS_Store
Thumbs.db
.vscode/
.idea/

# Next.js
/.next/
/out/
/node_modules/
*.tsbuildinfo

# Env files (never commit real secrets; env.example documents var names only)
.env
.env*.local

# Vitest / coverage
/coverage/
